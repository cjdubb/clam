
# Query: [name]

Purpose: [what question this query answers, in one line]
Tool: [Datadog | CloudWatch Logs Insights | Splunk | Loki | Kibana | psql | mysql | files/grep]
Query file: [relative path to the sibling query file, e.g. query.sql]
How to run: [where to paste the query, which time window/database to select; if the output is too large to paste in full, say what may be truncated and how, e.g. "first 50 rows of 12k"]

## Results

<!-- paste tool output below this line -->

## Interpretation
