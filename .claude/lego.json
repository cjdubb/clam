{
  "commands": {
    "test": "for t in plugins/*/scripts/*.test.sh plugins/*/lib/*.test.sh; do [ -f \"$t\" ] || continue; bash \"$t\" || exit 1; done; jq -e . .claude-plugin/marketplace.json >/dev/null",
    "lint": "bash scripts/executable-lint.sh && bash scripts/marketplace-lint.sh"
  },
  "models": {
    "testWriter": "sonnet",
    "implementer": "sonnet"
  },
  "testPatterns": ["*.test.sh"],
  "delivery": {
    "mode": "main-prs"
  }
}
