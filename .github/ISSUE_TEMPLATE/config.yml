# Contract: B03 issue-template-config (plan 001-repo-issue-template)
# Behavior:
#   Issue-chooser configuration: disables blank issues so every new issue
#   goes through one of the two forms (feature.yml, bug.yml).
# Inputs: n/a (static config read by GitHub).
# Outputs:
#   Exactly one key: `blank_issues_enabled: false`. No `contact_links`.
# Errors: n/a.
# Invariants:
#   - Parseable YAML; no contact links now or later without a plan change.
# Edge cases: n/a.

blank_issues_enabled: false
