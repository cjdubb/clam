{
  "commands": {
    "test": "for t in plugins/*/scripts/*.test.sh plugins/*/lib/*.test.sh; do [ -f \"$t\" ] || continue; bash \"$t\" || exit 1; done",
    "lint": "bash scripts/marketplace-lint.sh && bash scripts/executable-lint.sh && bash scripts/readme-lint.sh && bash scripts/issue-template-lint.sh"
  },
  "models": {
    "testWriter": "sonnet",
    "implementer": "sonnet"
  },
  "testPatterns": [],
  "delivery": {
    "mode": "main-prs"
  }
}
