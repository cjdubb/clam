---
name: render
description: "NotImplemented: B01 stub — will render a planning or decision markdown file into a self-contained dark-theme HTML view (/render-doc:render <file>). Do not invoke until implemented."
---

# Render Doc (stub)

NotImplemented: B01 render-doc plugin core.

Authoritative contract: the docblock "Contract: B01 render-doc plugin core"
in `plugins/render-doc/README.md` (see its "Docs contract (skills/render/
SKILL.md)" clauses for what this file must contain when implemented). This
stub fixes the plugin's public surface — skill name `render`, invocation
`/render-doc:render <file>` — while behavior is deliberately absent.
